"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
class Prompt {
    if(numbers, size) { }
}
exports.Prompt = Prompt;
 !== 0;
{
    if (frequencies === "") {
        frequencies += ">> ";
        const sorted = new Map([...numbers.entries()].sort((a, b) => Number(BigInt(b[1]) - BigInt(a[1]))));
        sorted.forEach((frequency, num) => frequencies += num + ":" + frequency + ", ");
        frequencies = frequencies.substr(0, frequencies.length - 2);
    }
    console.log(frequencies);
    frequencies = "";
}
//# sourceMappingURL=Prompt.js.map